---
interface Props {
	date: Date;
    year?: boolean;
}

const { date, year = true} = Astro.props;
---

<time datetime={date.toISOString()}>
	{
		date.toLocaleDateString('nl-nl', {
			year: year ? 'numeric' : undefined,
			month: 'long',
			day: 'numeric',
		})
	}
</time>
